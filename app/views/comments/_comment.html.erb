<small>
  <p><%= comment.body %></p>
</small>

<small>
  <p><%= image_tag(comment.user.avatar.tiny.url) if comment.user.avatar? %>
  submitted <%= time_ago_in_words(comment.created_at) %> ago by 
  <%= comment.user.name %></p>
</small>
<br/>